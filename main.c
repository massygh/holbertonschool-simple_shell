#include "shell.h"

/**
 * main - Entry point for the Simple Shell program.
 * Description: Initializes the shell and starts the shell loop.
 * Return: Always 0.
 */
int main(void)
{
	run_shell();
	return (0);
}
